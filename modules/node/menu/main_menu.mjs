import Menu from "./menu.mjs";
import MenuElement from "./element.mjs";

const mainMenu = {
    elements: [
        new MenuElement("Play", 0, true),
        new MenuElement("Options", 0, true),
        new MenuElement("Credits", 0, true)
    ]
};